WHENEVER SQLERROR EXIT 1;

SET ECHO ON;
SET SERVEROUTPUT ON;
SET TIMING ON;

WHENEVER SQLERROR EXIT 1;

DELETE FROM DM.DW_MAESTRA_POSTPAGO@DBL_DWM;

COMMIT;

INSERT INTO DM.DW_MAESTRA_POSTPAGO@DBL_DWM
(
MSISDN, 
CUSTOMER_ID, 
CO_ID, 
NRO_DOCUMENTO, 
FCH_ACTIVACION, 
STATUS, 
POSTPAGO, 
TIPO_DOCUMENTO, 
MES, 
REASON_STATUS
)
SELECT 
MSISDN, 
CUSTOMER_ID, 
CO_ID, 
NRO_DOCUMENTO, 
FCH_ACTIVACION, 
STATUS, 
POSTPAGO, 
TIPO_DOCUMENTO, 
MES, 
REASON_STATUS
FROM DWM.DW_MAESTRA_POSTPAGO
WHERE MES='&1';

COMMIT;

EXIT;
